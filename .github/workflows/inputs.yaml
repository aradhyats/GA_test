name: Manual Docker Build & Push

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Enter the Docker tag version (e.g., v1.0.0)"
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4