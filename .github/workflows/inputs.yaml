name: Manual Docker Build & Push

on:
  workflow_dispatch:
    inputs:
      tag_name:
      name:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: run the command on ubuntu
        run: | 
            echo "Hi I am Aradhya TS" 
            echo "Tag_name: ${{inputs.tag_name}}" 